# Python
__pycache__/
*.py[cod]

# Derived, disposable index cache (regenerated by problem_index.py)
*index*.json
index.json

# Operation log written by the write modes (per-machine, not source)
perspirate-log.md

# Local agent config (not part of the skill; the installer handles deployment)
.claude/

# Editor / OS cruft
.DS_Store
.vscode/
.idea/
