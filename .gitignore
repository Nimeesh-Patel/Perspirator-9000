# Python
__pycache__/
*.py[cod]

# Derived, disposable index cache (regenerated by problem_index.py)
*index*.json
index.json

# Historical operation log from pre-v1 write operations (per-machine, not source)
perspirate-log.md

# Local agent config (not part of the skill; the installer handles deployment)
.claude/

# Editor / OS cruft
.DS_Store
.vscode/
.idea/
